### Solution contents
Two directories are provided *scripts* and *src*.

The main file *classifier.py* is in *src* directory.

All other files are scripts are additional to showcase all experiments conducted for finding more sophisticated and refine solution, which are also part of additional items.